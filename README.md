# AfriLink

Professional networking platform for African CEOs built with Next.js, Prisma, and PostgreSQL.

Repository scaffold created.

Backend
-------

The `backend/` folder contains a Next.js App Router API and a Prisma schema. Configure `backend/.env` and run `prisma generate`.

